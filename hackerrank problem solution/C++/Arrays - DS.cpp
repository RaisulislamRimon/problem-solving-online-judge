#include <iostream>

using namespace std;

int main()
{
    typedef long long ll;
    ll total, i;
    string s;
    cin >> total;

    for (i = 0; i < total; i++)
        cin >> s[i];

    //for (i = total; i >= 0; i--)
    while (i--)
        cout << s[i] << " ";

}
